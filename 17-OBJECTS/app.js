const car = {
    make: 'Toyota',
    model: 'Corolla',
    colour: 'blue',
};

let myString = `I drive a ${car.colour} ${car.make} ${car.model}.`;

console.log(myString);

const book = {
    title: 'The Great Gatsby',
    author: 'F. Scott Fitzgerald',
    pages: 180,
};

console.log(`"${book.title}" by ${book.author} has ${book.pages} pages.`);
