import './Navbar.css';

function Navbar(){
    return(
        <nav>
            <a href="#">Home</a>
            <a href="#">About</a>
            <a href="#">About</a>
        </nav>
    );
}

export default Navbar;